node_modules/
dist/
.DS_Store

# Réglages propres à une machine (adresse d'un backend local…). Les fichiers
# .env.development / .env.production, eux, SONT versionnés : ils ne contiennent
# aucun secret et rendent le build reproductible. Voir .env.example.
.env
.env.local
.env.*.local
.env.production